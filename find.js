const numbers = [12, 32, 43, 65, 76, 12];
const findNumber = numbers.find(x => x > 10)
console.log(findNumber);

//  if i need a triangle it will return first triangle of the array 

const arr = ['triangle', 'moule', 'square', 'noman']
const tr = arr.find(p => p === 'triangle')
console.log(tr);